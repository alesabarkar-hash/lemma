ИГРОВОЙ ДВИЖОК ЛЕММЫ · версия 1.0

Весь движок находится в index.html, раздел
«ИГРОВОЙ ДВИЖОК ЛЕММЫ · фундамент».
Точка входа — глобальный объект Lemma.

ПОДСИСТЕМЫ
  Lemma.state       единое состояние игры
  Lemma.events      диспетчер событий
  Lemma.progress    расчёт прогресса
  Lemma.save        сохранения с версией
  Lemma.world       открытия мира
  Lemma.anim        очередь эффектов
  Lemma.daily       ежедневный мир

СОБЫТИЯ
  lesson_completed  word_learned      topic_completed
  lemma_received    area_unlocked     achievement_unlocked
  pet_equipped      item_bought       daily_event_started

ПОДПИСКА
  Lemma.events.on("topic_completed", d => { ... });
  Lemma.events.on("*", (d,e) => console.log(e.name));

ЧТЕНИЕ СОСТОЯНИЯ
  Lemma.state.lemmas          Lemma.state.level
  Lemma.state.companion       Lemma.state.items
  Lemma.state.words           Lemma.state.topicsDone
  Lemma.state.zones           Lemma.state.achievements
  Lemma.state.snapshot()      всё сразу

ОТКРЫТИЯ МИРА
  Lemma.world.unlockRegion("north")
  Lemma.world.unlockBridge("core_north")
  Lemma.world.unlockConstellation("family")
  Lemma.world.unlockWorkshop("grammar")
  Lemma.world.unlockPet("spark")
  Lemma.world.unlockBuilding("Archive")
  Lemma.world.evaluate()   пересчёт всех правил

ОЧЕРЕДЬ ЭФФЕКТОВ
  Lemma.anim.reward({lemmas:20, constellation:"family", zones:["north"]})
  Lemma.anim.setHandler(step => { ...визуал...; return Promise });

ПРОВЕРКА
  Кабинет → «Игровой движок» → кнопка «Проверить движок»
